languageCode = 'en-us'
title = 'Tracker'
baseURL = 'http://leak.info.gf/'
enableGitInfo = true

[frontmatter]
  lastmod = ['lastmod', ':fileModTime', ':git', ':default']

[params]
  favicon = '/icon.svg'

[taxonomies]
  artist = 'artists'
