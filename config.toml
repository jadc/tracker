languageCode = 'en-us'
title = 'Tracker'
baseURL = 'http://leak.info.gf/'
enableGitInfo = true

[frontmatter]
  lastmod = ['lastmod', ':git', ':fileModTime', ':default']

[params]
  favicon = '/icon.svg'

[taxonomies]
  artist = 'artists'
